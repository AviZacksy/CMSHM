import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "MediCare Hospital - Advanced Healthcare Services",
  description: "Leading medical hospital providing world-class healthcare services, specialized treatments, and compassionate care. Book appointments with expert doctors.",
  keywords: "hospital, healthcare, medical services, doctors, appointments, emergency care",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  );
}
